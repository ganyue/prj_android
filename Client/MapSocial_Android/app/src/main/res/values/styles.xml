<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar"/>

    <style name="FastBoot" parent="android:Theme">
        <item name="android:windowBackground">@mipmap/bg_splash_default</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
